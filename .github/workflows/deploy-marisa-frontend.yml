name: Deploy Marisa Frontend

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: [self-hosted, marisa-frontend]

    steps:
      - name: Ejecutar deploy completo de Marisa
        run: /root/deploy-marisa.sh